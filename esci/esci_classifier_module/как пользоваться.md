1. скопировать папку `esci_classifier_module`в папку проекта

2. если надо:
```{bash}
pip install scikit-learn transformers torch pymorphy3 pandas numpy joblib
```

3. и потом где-то использовать с импортом фукнции:
```Python
from esci_classifier_module.predict_esci import predict_esci  
  
print(predict_esci("энергетик без сахара", "энергетик самокат, без сахара, с соком малины, 0,33 л"))
```